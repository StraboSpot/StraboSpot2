import {StyleSheet} from 'react-native';
import * as themes from '../../../shared/styles.constants';

const customEndpointStyles = StyleSheet.create({
  checkBox: {
    alignItems: 'center',
    flexDirection: 'row',
  },
});

export default customEndpointStyles;
