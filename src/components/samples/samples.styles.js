import {StyleSheet} from 'react-native';
import * as themes from '../../themes/ColorThemes';

const samplesModalStyle = StyleSheet.create({
  samplesContainer: {
    // height: 400,
    flex: 6,
    backgroundColor: 'white',
    //position: 'absolute',
    borderBottomRightRadius: 20,
    borderBottomLeftRadius: 20,
    //top: 50
    width: '100%',
    // height: 200,
    // marginBottom: 25
  },
});

export default samplesModalStyle;
