import React from 'react';
import {Text, View} from 'react-native';

import {TagDetailNotebook} from '../tags';

const TagsSpotListNotebook = (props) => {
  return (
    <React.Fragment>
      <TagDetailNotebook/>
    </React.Fragment>
  );
};

export default TagsSpotListNotebook;
